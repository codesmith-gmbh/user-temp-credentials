(ns codesmith-gmbh.user-temp-credentials-test
  (:require [clojure.test :refer :all]
            [codesmith-gmbh.user-temp-credentials :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
